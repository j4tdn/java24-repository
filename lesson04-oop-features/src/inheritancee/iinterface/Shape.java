package inheritancee.iinterface;

public interface Shape {
	// 1 hàm trong interface mặc định có 2 từ khoá public abstract
	// hàm bên dưới có phần khai báo, không có phần thân hàm
	// --> hàm trừu tượng, abstract method
	void calcS();
}
