encapsulation = tính chất đóng gói 

Định nghĩa 
--------------
Tính chất đóng gói được được thể hiện qua các access modifier, nếu 
thuộc tính, phương thức có access modifier là 

+ private: nó chỉ sử dụng trong phạm vi của class

+ public: được sử dụng ở bất kì class, package nào trong dự án

+protected: được sử dụng trong package hiện tại
		  : được phép sử dụng ở Class con bên ngoài package
			
+         : được sử dụng trong package hiện tại 
